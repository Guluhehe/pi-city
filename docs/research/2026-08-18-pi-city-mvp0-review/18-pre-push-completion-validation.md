# Pi City 四章节扩展：推送前完成度验收

**核验时间：** 2026-08-18  
**核验对象：** 本地 `main`，以 `17-chapters-two-to-four-expansion-contract.md` 为验收基准。  
**结论：** 四章节城市活动已达到**可推送的功能完成度**。本轮核验发现电影委托的“两格小包”此前只存在于叙事文案中，因此已在推送前补入纯状态、UI 和自动测试；完成本文件后的最终门禁将重新执行。

## 契约覆盖情况

| 契约项 | 验收结果 | 实现与证据 |
|---|---|---|
| 根路径从城市故事开始，而非旧菜单 | 通过 | `CinematicCity` 默认模式为 `hub`；`CityStoryHub` 直接呈现心愿码头和第一件可办心愿。 |
| 12 件居民心愿按四章连续解锁 | 通过 | `CITY_MISSIONS` 包含 12 个任务；`campaign-chapters.test.ts` 从灯塔推进至星光节，逐项验证锁定与解锁。 |
| 每件新委托可走观察 → 出发 → 带回 → 重想 → 回信 | 通过 | 章节二至四的 9 件委托由 `makeSequentialMission()` 接入同一纯 reducer；每项在自动测试中完成至 `complete`。 |
| 温和绕路与可选发现 | 通过 | 每项顺序委托均有 `detour` 与 `optional` 路线，并在返回时向 `CityCampaign` 写入教程发现册。 |
| 故事不冒充真实运行 | 通过 | 城市活动、任务状态和3D背景一律保留 `source: 'tutorial'`；城市故事页明确说明不是 Trace；没有对 `SemanticTrace` 的写入路径。 |
| 第二章两格小包 | 通过（本轮补齐） | `CityMissionState.satchel` 在电影委托中以两格状态保存两条决定性发现；办事手账显示已占用与空格；自动测试验证填充顺序。 |
| 第三章行动后确认与改路 | 通过 | 种子任务要求行动后回花园确认；感谢卡和风车提供新证据改写初始路线及快/慢的温和绕路。 |
| 第四章顺序、矛盾线索、节日收束 | 通过 | 两张心愿单、雾中船铃、星光节依次实现先后取舍、矛盾线索再核验和城市物件收束。 |
| 章节化信任图与发现册 | 通过 | `CityStoryHub` 显示 4 个章节印记、每章 `0–3` 信任进度、12 件心愿卡和按章分组的未知/已得物件书架。 |
| 档案馆的诚实边界与概念墙 | 通过 | `CityArchive` 并列 Story / Concept / Evidence Run，完成数驱动无术语概念墙与最近物件托盘。 |
| 主题3D地标与语义镜头 | 通过 | `FountainStoryScene` 为 12 个主题定义焦点或地标；路线文字映射到不同地点和旅行时间，Pi 归来按结果类别携带不同色彩物件。 |

## 关键路径与视觉核验

| 路径 | 状态 | 审查结论 |
|---|---|---|
| 灯塔 | 通过 | 长路径审计已覆盖观察台、工具坊、花园、回信码头的计划、抵达、归来，并成功返回码头。 |
| 邮包 | 通过（已覆盖前半段） | 旧图书馆的完整出发—带回循环经截图确认。跨任务长审计在后段因无头 WebGL 和截图累积耗时停止，未发现状态机或界面异常。 |
| 喷泉 | 通过（既有专属链） | 喷泉继续使用独立 D 机制；共享3D场景已支持其计划、出发、行动和确认节奏。 |
| 第二至第四章 | 通过 | 使用章节预览路径捕获每章的码头、抵达和观察选择；风筝、种子和双心愿单均进入真实港口场景，而非静态页面。 |
| Pi 归来表演 | 通过（本轮精修） | 归来终点、相机侧位和发光强度已调整，Pi 与带回物不再与喷泉水雾混成过曝白团。 |

## 质量门禁

本次核验期内，完成度补齐前的完整门禁已通过：**63 项测试通过、TypeScript 通过、Vite 生产构建通过**。补齐两格小包后，针对性校验已通过，测试数升至 **64**；在推送前仍须重新运行完整 `npm run check:ci`，确保最终工作树的门禁证据与此验收结论一致。

## 如实保留的后续工作

> 下列项目不阻塞本次“全部章节已初步实际跑通”的目标，但不应被表述成已经完成。

| 后续项 | 原因 | 建议优先级 |
|---|---|---|
| 真实玩家试玩 | 当前验证基于自动化状态机和视觉捕获，尚未观察非技术玩家是否自然理解每次“该让 Pi 查什么”。 | P1 |
| 两格小包的主动换装/丢弃 | 当前电影委托准确显示并记录两条决定性发现；可选物明确留在码头，但尚未提供把多件候选物拖入/替换的背包整理交互。 | P1 |
| 本地持久化 | 刷新页面会从新的教学活动开始，符合当前无账号/无云端范围，但不保留城市进度。 | P2 |
| 长程视觉审计稳定性 | 三任务合并脚本在资源受限的无头渲染环境中时间较长；应改为分任务独立运行，以缩短反馈和便于定位。 | P2 |

## 推送前待办

1. 对本轮小包补齐执行完整持续集成门禁。
2. 将补齐代码、测试与本验收记录提交到本地分支。
3. 在 GitHub 凭据可用后推送 `main`，并核对远端提交 SHA。

### 电影小包视觉回归补充

`demo-recordings/chapter-previews/cinema-satchel.png` 已验证电影委托的真实任务页：首条“雨云照片”进入左侧格位，第二格以城市语言提示“留给下一件真正有用的发现”，同时右侧仍保留普通办事手账。该信息层级使“两格小包”可被看到、理解和在接下来的路线选择中使用，而没有遮挡观察选择层或 Pi 的3D场景。
